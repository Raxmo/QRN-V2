#version 450 core
layout(local_size_x = 8, local_size_y = 8) in;
layout(std430, binding = 0) buffer Grid
{
    float values[32][32];  // height x width
};

void main() {
	ivec2 ppos = ivec2(mod(gl_GlobalInvocationID.xy, 32));

	values[ppos.y][ppos.x] = float(min(mod(floor(ppos.x), 31.0), mod(floor(ppos.y), 31.0)) == 0.0);

	if(ppos.x == 14 && ppos.y > 2)
	{
		values[ppos.y][ppos.x] = 1.0;
	}
}